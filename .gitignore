# Local design reference assets (not for the repo)
design reference/

# Pre-redesign backup copy
Backup/

# Secrets / environment (never deploy-overwrite production)
api/config.php
.env
.env.*
!.env.example
